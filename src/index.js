import React from "react";
import ReactDOM from "react-dom/client";
import './style.scss'
import Form from "../src/Components/Form"

const root = ReactDOM.createRoot(document.getElementById("sass"))

root.render
(
    <Form />
)